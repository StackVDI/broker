%h1 Groups
%p You can manage groups in this page

%button.btn.btn-info
  =link_to 'Add role', new_role_path, :class => 'btn.btn.info', class: 'btn-info'
%br

%table.table.table-bordered.table-hover
  %thead
    %th
      Name
    %th 
      Actions
  %tbody
    - @groups.each do |group| 
      %tr
        %td
          =group.name
        %td
          %button.btn.btn-mini.btn-info 
            =link_to 'See users', administration_users_from_group_path(:id => group.id), :class => 'btn-info', id: "see_users_from_group_#{group.id}"
          %button.btn.btn-mini.btn-info 
            =link_to 'Edit', edit_role_path(:id => group.id), :class => 'btn-info', id: "edit_role_#{group.id}"
          %button.btn.btn-mini.btn-danger
            =link_to 'Delete', group,
              class: 'btn-danger', method: :delete, data: { confirm: 'Are you sure?' }, 
              id: "delete_role_#{group.id}"




      
